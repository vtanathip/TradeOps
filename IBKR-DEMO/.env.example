# Interactive Brokers — TWS paper trading.
# See verify_ibkr.py header for the one-time TWS API setup. Copy to .env to override.
IBKR_HOST=127.0.0.1
IBKR_PORT=7497        # 7497 = TWS paper, 7496 = TWS live, 4002 = IB Gateway paper
IBKR_CLIENT_ID=1      # any unused id; bump if "client id already in use"
IBKR_SYMBOL=AAPL      # instrument to test
